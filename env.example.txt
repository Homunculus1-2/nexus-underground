# Railway avtomatik beradi (PostgreSQL plugin qo'shganda)
DATABASE_URL=postgresql://user:password@host:5432/database

# Admin hisob ma'lumotlari
ADMIN_USERNAME=admin
ADMIN_PASSWORD=parolingni_o_zgartir

# JWT secret — uzun, random string bo'lsin
JWT_SECRET=bu_yerga_uzun_random_string_yoz_kamida_32_belgi

# Server porti (Railway avtomatik beradi)
PORT=3000
